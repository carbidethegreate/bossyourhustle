# Database settings
DB_HOST=localhost
DB_PORT=3306
DB_USER=carbide
DB_PASS=CHANGE_ME
DB_NAME=byhdb_

# Session secret
SESSION_SECRET=CHANGE_ME_LONG_RANDOM

# Default admin credentials (only used for initial seeding if no user exists)
ADMIN_USER=admin
ADMIN_PASS=StrongPass123

# API keys (optional; users can input their own)
ONLYFANS_API_KEY=
OPENAI_API_KEY=
